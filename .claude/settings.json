{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in */backend/engine/*.py|*/backend/bench/*.py) (cd /home/user/SIH26164/backend && uv run ruff check \"$f\" && uv run mypy --strict \"$f\") ;; esac; } 2>&1"
          }
        ]
      }
    ]
  }
}
